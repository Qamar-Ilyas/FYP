@model System.Data.DataTable
@{
    ViewBag.Title = "Index";
}
<br>
<h2 style="padding-top:20px;padding-left:220px;padding-bottom:20px;background-color:lightblue"><b>Sadqa And Zakat Collection And Distribution</h2></br>
<h2 style="padding-left:400px;padding-bottom:45px"><b>Welcome Dear Admin</h2></b>
<h2>Add Donor</h2>
<table class="table table-bordered table-striped">
    <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Cnic</th>
        <th>ContactNo</th>
        <th>Address</th>
        <th>Password</th>
    </tr>
    @for (int i = 0; i < Model.Rows.Count; i++)
    {
        <tr>
            <td>@Model.Rows[i][1]</td>
            <td>@Model.Rows[i][2]</td>
            <td>@Model.Rows[i][3]</td>
            <td>@Model.Rows[i][4]</td>
            <td>@Model.Rows[i][5]</td>
            <td>@Model.Rows[i][6]</td>
            <td>
                <a href="@Url.Action("Edit","Zakat", new {id=Model.Rows[i][0] })">Edit</a>
                <a href="@Url.Action("Delete","Zakat", new {id=Model.Rows[i][0] })">Delete</a>
            </td>
        </tr>
    }
</table>
<a href="@Url.Action("Creates","Welcome")">Add Donee</a>